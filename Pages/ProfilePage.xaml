<Page x:Class="ISIP323_Khachatryan_WPF.Pages.ProfilePage"
      Loaded="Page_Loaded"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ISIP323_Khachatryan_WPF.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ProfilePage">

    <Grid Background="Black">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Padding="10" VerticalAlignment="Center">
            <StackPanel>

               
                <TextBlock Text="Имя пользователя:" Margin="10" FontSize="18" Foreground="White" FontWeight="Bold"/>
                <TextBlock x:Name="TxtName" FontSize="18" FontWeight="Bold" Foreground="White" Margin="10,0,10,10"/>

                <TextBlock Text="Логин" Margin="10" Foreground="White" FontWeight="Bold" FontSize="18"/>
                <TextBlock x:Name="TxtLogin"  FontSize="18" FontWeight="Bold" Foreground="White" Margin="10,0,10,10"/>

                <TextBlock Text="Пароль" Margin="10" Foreground="White" FontWeight="Bold" FontSize="18"/>
                <TextBlock x:Name="TxtPassword" FontSize="18" FontWeight="Bold" Foreground="White" Margin="10,0,10,10"/>

                <Button Content="Выйти из аккаунта" Background="Blue" Foreground="White" FontSize="18" FontWeight="Bold" Margin="10" BorderThickness="0" Click="BtnLogout_Click"/>
                <Button Content="На главную" Background="Blue" Foreground="White" FontSize="18" FontWeight="Bold" Margin="10" BorderThickness="0" Click="BtnMain_Click"/>
            </StackPanel>
        </Border>

        <Grid Grid.Column="1" Margin="30,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Мои билеты" FontSize="22" FontWeight="Bold" Foreground="White" Margin="10" HorizontalAlignment="Center"/>

            <ListView x:Name="LViewTickets" Grid.Row="1" Background="Black" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled" Margin="10" >
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="Black" BorderThickness="2" Margin="10" Padding="15" Background="Blue" >
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel>
                                    <TextBlock Text="{Binding Sessions.Movies.Title}" FontSize="18" FontWeight="Bold" Foreground="White"/>

                                    <StackPanel Orientation="Horizontal" Margin="5">
                                        <TextBlock Text="{Binding Sessions.DateTime, StringFormat='dd MMMM HH:mm'}" Foreground="White" FontWeight="Bold"/>
                                        <TextBlock Text=" | " Foreground="White"/>
                                        <TextBlock Text="{Binding Sessions.Halls.Name}" Foreground="White" FontWeight="Bold"/>
                                    </StackPanel>
                                </StackPanel>

                                <Border Grid.Column="1" Background="DarkBlue" CornerRadius="5" Padding="10">
                                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <TextBlock Text="РЯД / МЕСТО" FontSize="10" Foreground="White" FontWeight="Bold"/>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                            <TextBlock Text="{Binding Seats.RowNumber}" FontWeight="Bold" FontSize="20" Foreground="White"/>
                                            <TextBlock Text=" - " FontWeight="Bold" FontSize="20" Foreground="White"/>
                                            <TextBlock Text="{Binding Seats.SeatNumber}" FontWeight="Bold" FontSize="20" Foreground="White"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <TextBlock x:Name="TxtNoTickets" Text="У вас нет купленных билетов." 
                       Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" 
                       Foreground="White" FontSize="16" Visibility="Collapsed"/>
        </Grid>
    </Grid>
</Page>
